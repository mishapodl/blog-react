import React, { Component } from "react";
import PropTypes from "prop-types";
import "./News.scss";

class News extends Component {
  static propTypes = {
    prop: PropTypes
  };

  render() {
    return <div />;
  }
}

export default News;
