import React, { Component } from "react";
import PropTypes from "prop-types";
import "./Post.scss";

class Post extends Component {
  static propTypes = {
    prop: PropTypes
  };

  render() {
    return <div />;
  }
}

export default Post;
