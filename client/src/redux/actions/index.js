import {
  loadPosts,
  getIdPost,
  sendComment,
  deleteComment
} from "./actions-posts";
import { loadUser } from "./actions-auth";

export { loadPosts, getIdPost, sendComment, deleteComment, loadUser };
